plugins {
    id "com.gradle.plugin-publish" version "0.12.0"
    id 'java-gradle-plugin'
    id 'groovy'
    id 'maven-publish'
    id "idea"
}

repositories {
    maven {
        url "https://plugins.gradle.org/m2/"
    }
    mavenLocal()
    jcenter()
}

dependencies {
    api 'com.jcraft:jsch:0.1.55'

    testImplementation('org.spockframework:spock-core:1.2-groovy-2.5') {
        exclude group: 'org.codehaus.groovy'
    }
    testImplementation('cglib:cglib-nodep:2.2')
    testImplementation('org.objenesis:objenesis:1.2')
    testImplementation gradleTestKit()
}

buildScan {
    termsOfServiceUrl = 'https://gradle.com/terms-of-service'
    termsOfServiceAgree = 'yes'
}

group = "edu.wpi.first"
archivesBaseName = "EmbeddedTools"
version = "2020.12.23" // YYYY.MM.DD(revision)

sourceCompatibility = 11
targetCompatibility = 11

pluginBundle {
    website = 'https://github.com/wpilibsuite/EmbeddedTools'
    vcsUrl = 'https://github.com/wpilibsuite/EmbeddedTools'
    description = 'Additions to the model-based DSL for deploying Java and Native projects to remote targets'
    tags = ['remote', 'target', 'deploy', 'java', 'native']
}

gradlePlugin {
    plugins {
        embeddedTools {
            id = 'edu.wpi.first.EmbeddedTools'
            displayName = 'EmbeddedTools'
            implementationClass = 'edu.wpi.first.embeddedtools.EmbeddedTools'
        }
    }
}

tasks.withType(JavaCompile) {
    options.compilerArgs << '-Xlint:unchecked'
    options.deprecation = true
}

wrapper {
    gradleVersion = '6.8.1'
    distributionType = Wrapper.DistributionType.BIN
}
